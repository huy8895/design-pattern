package flyweight.concept;

/**
 * ConcreteFlyweight : triển khai các phương thức đã được định nghĩa
 * trong Flyweight. Việc triển khai này phải thực hiện các khả năng của trạng thái nội
 * tại. Đó là dữ liệu phải không thể thay đổi (unchangeable) và có thể chia sẻ
 * (shareable). Các đối tượng là phi trạng thái (stateless) trong triển khai này. Vì vậy,
 * đối tượng ConcreteFlyweight giống nhau có thể được sử dụng trong các ngữ cảnh khác nhau.
 */
public class ConcreteFlyweight implements Flyweight{

}
